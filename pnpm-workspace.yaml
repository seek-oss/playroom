allowBuilds:
  '@parcel/watcher': true
  cypress: true
  esbuild: true
  unrs-resolver: true

overrides:
  # Installed via `eslint-config-seek`, brings in `jest` deps that we don't need
  # because we use Vitest.
  eslint-plugin-jest: '-'
