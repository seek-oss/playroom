---
'playroom': minor
---

Add support for vite. This allows user to configure vite as the bundler, by specifying either `bundler: 'vite'` or `viteConfig` options in their Playroom config.

Vite has to be installed separately by the user, and Playroom still defaults to webpack as the default.
