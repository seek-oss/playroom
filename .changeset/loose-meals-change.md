---
'playroom': patch
---

Editor: Ensure interactivity after toggling visibility via menu

Fixes an issue where the editor would remain `inert` when hidden via the "Hide Code" button and then shown via the menu.
